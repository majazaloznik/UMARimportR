% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_writing.R
\name{insert_new_category_relationship}
\alias{insert_new_category_relationship}
\title{Insert a new category relationship}
\usage{
insert_new_category_relationship(con, df, schema = "platform")
}
\arguments{
\item{con}{Database connection object}

\item{df}{A data frame with one row containing the following columns:
* id (integer): ID of the child category
* parent_id (integer): ID of the parent category
* source_id (integer): ID of the source these categories belong to}

\item{schema}{Character string specifying the database schema. Defaults to "platform"}
}
\value{
A data frame with one column 'count' indicating number of rows inserted
        (1 for success, 0 if relationship already exists)
}
\description{
Creates a parent-child relationship between two categories within the same source.
The relationship is defined by specifying which category is the child (id) and
which is the parent (parent_id).
}
\examples{
\dontrun{
df <- data.frame(
  id = 2,         # child category
  parent_id = 1,  # parent category
  source_id = 1
)
insert_new_category_relationship(con, df)
}

}
